version: '3'
services:
  my-spring-boot-app:
    image: my-spring-boot-app
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    networks:
      - my-spring-boot-app-network
networks:
  my-spring-boot-app-network:
    driver: bridge
